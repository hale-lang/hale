# `std::iter`

> std::iter::Lines — cursor-shape iteration over a newline-separated String.

The source for this module is the documentation; the page is regenerated by
`docs/scripts/regen-std-source.py` from the live stdlib source.

```aperio
{{#include ../../../crates/aperio-codegen/runtime/stdlib/iter.ap}}
```
