// a downstream tool F.10 reproducer — cross-seed type definition.
// Imported by ../qualified-type-fwd-app via `as gfx;`.
type Rect {
    x: Float = 0.0;
    y: Float = 0.0;
    w: Float = 0.0;
    h: Float = 0.0;
}
