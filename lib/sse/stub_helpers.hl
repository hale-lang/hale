// SSE stub — test injection helpers.
// DELETE THIS FILE (or rewrite against a real in-memory
// http::Response fixture) when real impl ships.

fn stub_enqueue_event(s: SseStream, event: String, data: String) {
    s._enqueue(SseEvent { event: event, data: data, id: "" });
}

fn stub_close(s: SseStream) {
    s._mark_closed();
}
