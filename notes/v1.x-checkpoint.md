# v1.x Implementation Checkpoint

**Purpose.** Persistent state for the v1.x followups list — survives
context compaction. A new session reads this file + the recent git
log to know what's done, what's in flight, and what's next.

The list is ordered through The Design's lens (see session of
2026-05-11): form-shaping first, then form-extending, then
parameter-populating, then stdlib.

---

## Status legend

- `[ ]` not started
- `[~]` in progress (active branch / uncommitted work)
- `[x]` completed (commit referenced)
- `[#]` blocked on a design question that needs user input

---

## Form-shaping (substrate-honesty gaps)

- `[x]` **1. Interpreter parity for F.22 capacity slots** — commit TBD
  - Value::Cell + LocusHandle.slots + SlotState (Pool/Heap)
  - eval.rs instantiate_locus walks LocusMember::Capacity
  - try_eval_capacity_slot_call intercepts self.X.method() shape
  - tests/f22_capacity_interpreter.rs (5 tests; round-trip, churn,
    cross-kind rejection, multi-slot coexist)
  - 439 / 0 workspace test count

- `[x]` **2. Cell content I/O** — commit TBD (struct cells only)
  - Codegen lower_expr: Cell<TypeRef> reads via struct GEP + load
  - Codegen Stmt::Assign: cell.field = v writes via struct GEP + store
  - Primitive cells (Cell<Int> etc.) reject field access with focused diagnostic
  - Interpreter SlotState carries elem_ty_name; acquire/alloc default-
    constructs Value::Struct when slot is struct-typed
  - read_field + assign_lvalue + descend gain Value::Cell arms
  - 4 codegen tests (struct write/read, heap field, primitive reject,
    Pool free-list-first-8-bytes-overwrites contract) + 2 interpreter
    parity tests. 445 / 0 workspace.
  - **v1 contract surfaced**: Pool release writes free-list pointer
    over the cell's first sizeof(void*) bytes — user code that
    acquires must rewrite struct fields before reading.

- `[ ]` **3. Recognition projection class proper backing** —
  design RESOLVED 2026-05-11; implementation pending

  **Design decision (Design-aligned, user-confirmed):**
  Recognition's storage discipline is an axis with FOUR named
  sub-modes — same pattern as projection class itself,
  schedule class, and F.22 slot kinds. The user *writes down*
  which commitment they're making per-locus.

  Syntax:
  ```
  : projection recognition(cap=N, <sub-mode>)
  ```

  Sub-modes:
  - `fixed_cell(bytes=N)` — each child fits in N bytes; overflow
    is a hard runtime error. Bounded state you can size up front.
  - `spillover(bytes=N)` — fixed N-byte cell + malloc fallback
    with one-time warning. Graceful degradation under load.
  - `summary_only` — children carry zero per-instance state; all
    allocations live in parent's arena.
  - `shared_slab(bytes=N)` — one bump arena of N bytes serves
    all children; per-child dissolve is no-op; whole slab frees
    at parent dissolve.

  **Default sub-mode if omitted: RESOLVED 2026-05-12 —
  required, no default.** Bare `: projection recognition`
  becomes a parse error. User MUST commit to a sub-mode at
  the declaration site. Forcing function is productive — same
  shape as the 2026-05-12 two-channel rule (locus methods
  can't declare fallible(E); user names which channel the
  failure lives in). `examples/14-projection-classes/main.ap`
  needs an update as part of v1.x-3 ship — currently uses
  bare recognition.

  **Ship cadence (v1 / v1.x):**
  - v1: parse all four sub-modes; codegen ships `fixed_cell` and
    `shared_slab` (cleanest implementations); reject `spillover`
    and `summary_only` with "v1.x pending" diagnostic (mirrors
    v1.x-4 surface pattern for `as_parent_for`).
  - v1.x: ship `spillover` (needs malloc-fallback + warning
    machinery).
  - v1.x or later: ship `summary_only` (needs type-system rule
    "no child arena allocation").

  **C runtime work:**
  - `lotus_recpool_fixed_*` — bitmap-backed fixed-cap pool of
    fixed-size cells; acquire/release O(1) via bitmap scan;
    no malloc in steady state.
  - `lotus_recpool_slab_*` — single bump arena with bitmap of
    "child boundaries" so individual child dissolve is no-op
    but parent dissolve frees the whole slab.

  **Codegen work:**
  - Extend ProjectionClass to carry sub-mode + params.
  - Route child-locus instantiation through the matching
    recpool primitive when parent is recognition-class.
  - Accept the existing `lotus_arena_t*` ABI on the child side
    by wrapping the recpool cell in a fixed-cap arena variant
    (children's body code stays projection-class-agnostic per
    the F.22 architectural invariant).

- `[x]` **4 + 4b. `as_parent_for ChildL`** — surface +
  runtime, both shipped.
  - v1.x-4 (surface; prior commits): AST + lexer + parser +
    typecheck validation (existence, slot-name match) +
    grammar.ebnf.
  - **v1.x-4b (runtime mechanic; commit `d50ab79` 2026-05-12):**
    - `__slot_borrowed_mask: i64` synthetic field on every
      locus struct (uniform layout, zero-init at instantiation).
    - At child instantiation: if `self.current_self` is set and
      parent has `as_parent_for <this_locus>`, copy parent's
      slot ptr into child's slot + OR the matching bit.
    - At emit_locus_arena_destroy: per-slot AND with bit
      constant + cond-br around the destroy call. Borrowed
      slots skip destroy (parent owns the underlying allocator;
      F.4 cascade ensures child dissolves first).
    - Codegen-side defensive kind+elem_ty validation at borrow
      swap. `@form(vec)` slots can't be borrowed (rejected).
    - Removed the prior "v1.x-4b pending" codegen reject.
    - Tests rewritten in tests/f22_as_parent_for_surface.rs
      (5 tests): compiles_and_runs, typecheck_rejects_unknown_
      locus, typecheck_rejects_mismatched_slot,
      codegen_rejects_kind_mismatch,
      child_uses_parent_allocator (end-to-end acquire +
      release inside child run() through borrowed slot).
  - Interpreter parity deferred — no current app/example uses
    as_parent_for; rollup path is `aperio build`.

- `[x]` **5. Slot-of-origin tracking on `Cell<T>`** — commit TBD
  - CodegenTy::Cell extended from `Cell(Box<T>)` to
    `Cell(Box<T>, Option<(locus_name, slot_name)>)`. Option<...>
    reserved for future generic-Cell<T> positions; v1 always Some.
  - Codegen acquire/alloc sets origin; release/free destructure +
    validate the origin matches the current slot. Cross-slot
    release rejects with "originated from X.Y — can only be
    released into the slot they came from" diagnostic.
  - Interpreter parity: Value::Cell already carried slot_locus +
    slot_name; release/free now validate at runtime with same
    diagnostic shape.
  - 1 codegen test (cross-slot rejected, was v1 no-op now hard
    error) + 1 interpreter parity test. 452 / 0 workspace.

---

## Form-extending (Design mechanics at new levels)

- **REMOVED 2026-05-12 — 6. Result + `?` operator**
  - Cut from the roadmap after a design pass. `?` is pure
    desugaring (`r.is_err { return r.err; }`); the load-bearing
    part is `Result<T, E>` as a discipline that forces callers
    to acknowledge errors at the type level.
  - Aperio already has failure-propagation-upward at the
    **locus** level via `bubble` / `on_failure` — the Design's
    failure-propagation-upward mechanic expressed structurally.
    Adding value-level Result would create a second, parallel
    mechanism for the same thing (parametric option for what
    is structurally already covered — exactly the shape The
    Design counsels against).
  - The current Aperio pattern is clean:
    - `parse_int(s)` returns `0` on failure (sentinel-with-default).
    - `can_parse_int(s)` is the explicit discriminator.
    Caller picks the appropriate pair — no type-system tax on
    the common case where 0-on-failure is fine.
  - No friction-log entry asks for value-level Result. If a
    future workload demands one, revisit the design call —
    but don't ship speculatively.

- `[#]` **7. Match exhaustiveness via case folding** —
  no concrete friction driver yet
  - check.rs `match_is_exhaustive` currently exhaustive on
    Bool (both literals) and Enum (all variants), permissive
    on Unknown / unrecognized names, requires catchall
    elsewhere. That's sound today.
  - The v1.x-7 framing was forward-looking — fold `0|1|2|3` +
    `4..=10` into `0..=10` and check coverage; let `0..=i64::MAX`
    skip catchall on Int. No friction-log entry calls for it.
  - Defer until a workload writes ranges that the current
    "require catchall" rule rejects unhelpfully.

- `[x]` **8. `type` records hold `fn(...)` fields** — commit TBD
  - Three fixes (not one):
    1. Parser: `parse_struct_field` + `parse_struct_init` use
       `expect_member_name` instead of `expect_ident`; extended
       `try_member_keyword_as_name` to admit capacity / birth /
       accept / run / drain / dissolve as field names.
    2. Codegen: `lower_external_method_call` gained a TypeRef +
       FnPtr-field branch ahead of the LocusRef path; GEPs the
       field, loads the fn pointer, emits indirect call via
       `emit_fnptr_indirect_call`.
    3. No typecheck change needed — once parser admitted the field
       and codegen had the dispatch path, existing `fn(...)` type
       representation was sufficient.
  - 3 codegen tests in `type_record_fn_pointer_field.rs`
    (zero-arg, int-arg with mixed struct payload, dispatcher
    pattern selecting among records).
  - Friction marked LANDED 2026-05-11.

---

## Parameter-populating (Design-neutral once the form fits)

- `[ ]` **9. Closures with capture (3c / T2.1)**
  - **GATED**: needs a Design answer on MS2 (every quantity assigned
    to one locus tower). Naive lexical capture lets values float.
  - Possible resolution: only capture locus-references + value-shape
    state; reject captures that would create floating quantities.
  - Defer until design pass complete.

- `[x]` **10. String interpolation** — commit TBD
  - Opt-in `f"..."` prefix (NOT plain `"..."`) so existing
    `.ap` sources with literal `{...}` content (JSON, etc.)
    keep working unchanged. Backward-compat preserved.
  - Lexer recognizes `f"..."`, splits body into FStringPart::Lit
    / Interp segments; `{{` / `}}` are literal braces.
  - Parser sub-parses each Interp body as a full Aperio
    expression and wraps in `to_string(...)`; whole f-string
    lowers to a left-folded `+` chain.
  - Works in both interpreter and codegen (no new lowering;
    just AST desugaring at parse time → existing string + and
    to_string paths).
  - 8 tests in `fstring_interpolation.rs` (ident, int, multi,
    dotted member, arithmetic expression, escaped braces,
    bare-interpolation, plain-string-backcompat).
  - Spec: tokens.md updated with f-string entry.

- `[x]` **11. Float → Int cast (narrowing, explicit)** — commit TBD
  - Surface: `Int(f)` — constructor-shaped call. Float arg
    truncates toward zero (LLVM fptosi); Int arg is identity;
    other types rejected so silent narrowing can't sneak in.
  - Codegen: `lower_int_cast_builtin` (fptosi to i64).
  - Interpreter: `builtin_int_cast` (`as i64` for finite, 0 for NaN).
  - 4 tests in `float_to_int_cast.rs` (positive/negative
    truncation, Int identity, arithmetic chain).

---

## Stdlib (substrate-tied first, leaves last)

- `[ ]` **12. Map (T1.2)** — pool-of-buckets on F.22; gates on #2 (cell I/O)
- `[ ]` **13. Vec (T1.1)** — heap-of-T on F.22; gates on #2
- `[ ]` **14. Rope / chunk-list** — heap-of-chunk; resolves
  `reader-list_item-quadratic-concat` friction
- `[x]` **15. String-builder primitive** — commit TBD
  - Resolves reader-list_item-quadratic-concat friction.
  - C runtime: `lotus_str_builder_new/append/len/finish` with
    doubling-realloc malloc buffer (initial cap 64, doubles).
    N appends → amortized O(N) total. finish() copies into the
    bus payload arena, frees the builder.
  - Codegen path-call dispatch: `std::str::builder_*` surface.
    Handle carrier type is Bytes (opaque — users only pass it
    between the builder fns).
  - Interpreter parity: Array-wrapping-Bytes shape for shared
    mutable access without a new Value variant.
  - 5 tests in `string_builder.rs` (round-trip, len-tracking,
    1000-appends, empty-finish, interleaved-work).
  - Rope (item 14) stays open as a future optimization (would
    let `s1 + s2` itself be O(1)); builder is the smaller
    surface that unblocks ferryman scale today.
- `[x]` **16. `parse_float`, `base64::decode`, `min`/`max` builtins** — commit TBD
  - `min` / `max` already in tree from m38 (codegen
    lower_math_builtin + runtime builtin_min/max).
  - New: `std::str::parse_float(s) -> Float` and
    `std::str::can_parse_float(s) -> Bool` — mirror parse_int's
    "0.0 on failure, paired bool predicate" contract.
  - New: `std::text::base64::decode(s) -> Bytes` — inverse of
    base64::encode. Whitespace tolerated; non-alphabet / wrong
    padding length returns empty Bytes.
  - C runtime additions: `lotus_str_parse_float`,
    `lotus_str_can_parse_float`, `lotus_text_base64_decode`.
  - Interpreter parity: built-in `std_str_parse_float`,
    `std_str_can_parse_float`, `std_text_base64_decode` via
    resolve_path; bonus: `len()` on Bytes now supported.
  - 7 tests in `parse_float_and_base64_decode.rs`.

---

## Notes for next session

- Workspace test count baseline before this session's work: 434 passing
- F.22 substrate + dispatch shipped this session (commits `70128ea`,
  `a811336`); spec sync at `f99d791`
- The friction log is the canonical source for "what's flaky" — check
  it before assuming a v1.x item is fresh
- Memory note `project_f22_v1_arc.md` has v1.x followup details
- Item ordering is Design-anchored (form before parameter); deviate
  only with explicit user OK

---

## Active task

(updated when work begins on each item)

**Current (post-2026-05-12 stability sweep):** Goal is language-
surface stability for app rollup. Existing apps/+examples/ build
clean today (verified 2026-05-12; only blocker is
`examples/fitter-applier-pair` multi-binary, intentional).

**Done this stability sweep:**
- v1.x-4b runtime mechanic (commit `d50ab79`).
- Two-channel rule lock-in: locus methods can't declare
  fallible(E) (commit `24ce7b6`). Locked as design enforcement,
  not a temporary limitation. 4 spec files updated +
  typecheck reject + 3 tests.

**Next in plan**: v1.x-3 (recognition pool backing). Handoff
doc at `notes/v1.x-3-handoff.md` — substantial work (C runtime
+ parser + typecheck + codegen + interp parity), recommended
fresh session.

**Pre-2026-05-12 history:** Pushed past the original v1.x list —
shipped 8 numbered items earlier (1 / 2 / 4 / 5 / 7 from earlier
+ 8 / 10 / 11 / 15 / 16 / 17-partial from the autonomous push)
plus follow-up primitives.

Remaining v1.x items (all design-gated):
- v1.x-9: closures with capture — MS2 invariant constraint.
- v1.x-12 / 13: Map / Vec stdlib — gated on generics or
  fixed-instance design call.
- v1.x-14: Rope — alt to v1.x-15, lower priority now that the
  string-builder ships.

Cut from roadmap:
- v1.x-6 (Result + `?`) — REMOVED 2026-05-12 after design pass.
  Locus-level `bubble` / `on_failure` already covers
  failure-propagation-upward structurally; adding value-level
  Result would duplicate that mechanism parametrically. The
  sentinel-with-discriminator pair (`parse_int` / `can_parse_int`)
  is the Aperio idiom — no type-system tax on the common case.

v1.x-17 (DONE 2026-05-11): F.22 Pool initial chunk size now
derives from sysconf(_SC_PAGESIZE) — fits one host page when
cell_stride permits, else falls back to the static-16 floor.
Recognition pool defaults are still pending (gated on v1.x-3
implementation).
**Last commit:** `f84566a` (v1.x-4 surface; also bundled
moa/* + lib/README.md files that appeared mid-session from
another process — see "concurrency note" below).

**Recent:**
- `24ce7b6` — v1.x-FORM-2 two-channel rule locked in:
  locus methods can't declare fallible(E). Design enforcement.
  4 spec files + typecheck reject + 3 tests.
- `d50ab79` — v1.x-4b: as_parent_for runtime mechanic.
  __slot_borrowed_mask + borrow swap + skip-destroy at
  dissolve. 5 tests.
- `211c00d` — spec reframe for PR6 two-channel semantics.
- v1.x-FORM-2 PR6 (commits `32e764d`..`d77ed29`) — fallible /
  fail / or end-to-end + PR5 finale (synth @form(vec) get/pop).
- `f84566a` — v1.x-4: as_parent_for language surface
  (predecessor; runtime = v1.x-4b above).
- v1.x-3 (recognition pool): DESIGN COMPLETE 2026-05-11 + sub-
  mode rule locked 2026-05-12 (required, no default). Impl
  pending — see `notes/v1.x-3-handoff.md`.

## Session log (2026-05-11 → 2026-05-12)

Shipped this session (in commit order):

1. `4f4043f` v1.x-1: F.22 interpreter parity
2. `1c39fbf` v1.x-2: cell content I/O (struct cells)
3. `f84566a` v1.x-4: as_parent_for surface (runtime mechanic = v1.x-4b)
4. `<earlier>` v1.x-5: slot-of-origin tracking
5. `<earlier>` v1.x-7: match exhaustiveness (deferred — no driver)
6. `4367f34` v1.x-8: type records hold fn(...) fields
7. `d50d242` v1.x-10: f-string interpolation
8. `4dbd168` v1.x-11: explicit Float → Int narrowing via Int(x)
9. `547dce8` v1.x-16: parse_float / base64::decode (min/max already shipped)
10. `f411751` v1.x-15: string-builder primitive (resolves quadratic-concat)
11. `c260e49` spec: v1.x followups catalog in stdlib / runtime / tokens
12. `4992a17` v1.x-17 (partial): F.22 Pool initial chunk = host page size
13. `325281e` spec: grammar.ebnf adds FSTRING_LIT production
14. `229a3da` v1.x checkpoint session log
15. `97647b0` stdlib: std::str::lower / upper + f-string nested-string fix
16. `4d90154` stdlib http: case-insensitive header lookup via std::str::lower
17. `70446be` friction: case-insensitive header lookup follow-up note
18. `86bccbb` stdlib cli: route upper() through std::str::upper
19. `dee4c44` stdlib: std::str::trim + std::str::replace
20. `e5a5b4f` spec: catalog std::str::trim and std::str::replace
21. `7f929bf` stdlib lang: trim() routes through std::str::trim
22. `77ab91a` v1.x checkpoint: extended session log
23. `b318b09` stdlib: std::str::repeat + pad_left + pad_right
24. `16ede23` docs: std::str mdbook page covers v1.x followup primitives
25. `d645689` docs: reference/lexical.md documents f-strings

Workspace test count: 503 passing at end of session
(up from 452 at start — 51 new tests across all new features).

Deferred with design notes (won't ship without a design call):
- v1.x-3: recognition pool (design resolved, implementation
  needs C-runtime work + parser extensions for sub-modes).
- v1.x-4b: as_parent_for runtime mechanic (bitmap mask).
- v1.x-9: closures with capture. MS2 invariant constraint.

Cut from roadmap (2026-05-12 design pass):
- v1.x-6: Result + ?. The locus tower's `bubble` / `on_failure`
  already covers failure-propagation-upward structurally; adding
  value-level Result would duplicate that mechanism parametrically.
  Aperio's idiom is sentinel-with-discriminator (`parse_int` +
  `can_parse_int`); no type-system tax on the common case.
- v1.x-12 / 13 / 14: Map / Vec / Rope as parametric stdlib
  types. Replaced by the `@form(...)` machinery from the
  proto-locus design pass. Aperio source code never says
  `Map<K, V>` / `Vec<T>` parametrically — collections are
  loci with form annotations. See
  `notes/agent-onboarding/aperio-design-philosophy.md` for the
  locked-in framing and `notes/proto-locus-design.md` for the
  worked example (`@form(vec)`) and roadmap.
- v1.x-17 (recognition pool side): gates on v1.x-3 impl.

Replacement work (proto-locus form machinery):
- v1.x-FORM-1: spec `@form(vec)` shape contract in detail.
  **DONE 2026-05-12** — `spec/forms.md` written (form annotation
  system in general + full `@form(vec)` contract: required
  capacity shape, synthesized methods including
  `fallible(IndexError)`, lowering strategy, bench protocol).
  Paired with `aperio-design-philosophy.md` rewrite of § 2 to
  introduce the `fallible(T)` value-level error protocol
  (axiom → motions → terminal binary; typed at value level,
  opaque at runtime). Memory: `project_fallible_error_model.md`.
- v1.x-FORM-2: implement `@form(vec)` end-to-end (annotation
  parsing + shape verification + C-runtime vec + codegen
  lowering). Also implements the `fallible(T)` / `fail` / `or`
  surface, since FORM-1's synthesized methods depend on it.

  **Spec scaffolding (2026-05-12):**
  - `spec/grammar.ebnf` — form_annotation, fallible_marker,
    fail_stmt, or_clause productions added.
  - `spec/tokens.md` — fallible/fail/or/raise as contextual
    keywords; `@` reclassified from "reserved no v0 meaning" to
    "annotation prefix"; `err` documented as implicit binding
    on `or` RHS (typecheck rule, not lexer rule).
  - `spec/forms.md` — full @form(vec) contract.

  **Codebase entry points** (surveyed 2026-05-12):
  - `crates/aperio-syntax/src/lexer.rs` (1231 LoC) — add
    contextual-keyword recognition. Same pattern as the
    existing `approx`/`within`/`pool`/`heap`.
  - `crates/aperio-syntax/src/ast.rs` (837 LoC) — add
    FormAnnotation, FallibleMarker, FailStmt, OrDispositionExpr
    nodes.
  - `crates/aperio-syntax/src/parser.rs` (2572 LoC) — productions
    per spec.
  - `crates/aperio-types/src/check.rs` (1625 LoC) — fallible
    tracking, "error not addressed" rule, `err` binding,
    `fail`-only-in-fallible-body rule, @form(vec) shape
    verification, method synthesis.
  - `crates/aperio-codegen/src/codegen.rs` (21836 LoC) — lower
    @form(vec)'s heap slot, lower synthesized push/get/pop/len/
    is_empty, lower fail/or raise/or substitute.
  - `crates/aperio-codegen/runtime/lotus_arena.c` (4074 LoC,
    single file) — add `lotus_vec_*` primitives alongside the
    arena code. Or split into a fresh `lotus_vec.c` file —
    decision pending.
  - `crates/aperio-runtime/src/eval.rs` + `value.rs` —
    interpreter parity for all of the above.

  **Recommended PR sequencing** (smallest viable slices):
  1. **PR1 — parser only.** Lex + parse @form / fallible / fail /
     or. AST nodes land; no semantic enforcement. Tests are
     parse-OK / parse-reject.

     **DONE 2026-05-12 (commit pending).**
     - AST: `LocusDecl.form: Option<FormAnnotation>`,
       `FnDecl.fallible: Option<TypeExpr>`, `Stmt::Fail`,
       `Expr::Or { inner, disposition: OrDisposition }`.
     - Parser: `@form(...)` prefix on `locus` (rejects other
       targets); `fallible(T)` after fn return type;
       contextual `fail <expr>;` statement inside fallible-body
       scope (tracked via `Parser.in_fallible_body`); contextual
       `or`-disposition postfix on expressions, right-associative
       for chains (`a() or b() or raise`).
     - All four new keywords (`fallible`/`fail`/`or`/`raise`)
       stay as Ident at the lexer level; parser uses positional
       contextual recognition.
     - 8 new parse tests covering: form-no-args, form-with-args,
       form-rejects-non-locus, form-rejects-unknown-@-keyword,
       fallible marker (with and without `->` return), fail
       inside fallible body, `or raise` / `or substitute` /
       `or handler(err)`, right-associative chain, contextual
       Ident usage outside keyword positions.
     - Typecheck / codegen / eval got minimal pass-through
       arms with explicit PR2/6/7 deferral diagnostics so the
       new AST nodes don't break the build.
     - 517 workspace tests passing (up from 503; +14 = 8 new
       parser tests + 6 unrelated tests that already shipped).
  2. **PR2 — fallible typecheck.** Fallible-return tracking +
     "error not addressed" + `err` binding + `fail` body-only
     check. Pure typecheck tests, no codegen.

     **DONE 2026-05-12 (commit pending).**
     - `Ty::Fallible { success, payload }` added to `ty.rs`;
       displays as `T fallible(E)`; not assignable to its
       success type without unwrap.
     - `FnSig.fallible: Option<Ty>` carries the payload from
       resolution. Call expressions to fallible fns produce
       `Ty::Fallible` automatically via
       `Checker::callee_fallible_payload`.
     - `Checker::check_expr_addressed` enforces "error not
       addressed" at the statement boundaries (Let, LetTuple,
       Assign, Return, Expr, Fail's payload). Sub-expression
       checks still use `check_expr` so `or`-disposition
       sees the raw `Ty::Fallible`.
     - `Checker.fallible_ctx: Option<(success, payload)>`
       pushed/popped around fallible fn bodies. `return`
       checks against success type; `fail` checks against
       payload type; `fail` outside the context is rejected.
     - `Expr::Or` typecheck: requires fallible inner;
       `Raise` returns success type; `Substitute` introduces
       implicit `err` binding (typed as payload) in the RHS
       scope and unifies RHS type with success type.
     - 10 new tests: error-not-addressed (let, expr-stmt),
       OK with `or raise` / `or 99`, substitute-type-mismatch,
       `err`-binding usage, `or` on non-fallible (diag),
       `fail` payload type matches, `fail` payload mismatch,
       right-associative chain typechecks.
     - 527 workspace tests passing (up from 517).
  3. **PR3a — @form(vec) shape check.** Capacity-shape
     verification only. Method synthesis split into PR3b.

     **DONE 2026-05-12 (commit pending).**
     - `Checker::check_form_shape` dispatched in
       `check_locus` after locus info is loaded.
     - `@form(vec)`: requires exactly one `heap` slot of any
       cell type T. Rejects: pool slot (different storage
       discipline diagnostic), missing capacity block, empty
       capacity, multiple slots, args (`@form(vec, ...)`),
       and `as_parent_for` override on the slot.
     - `@form(hashmap)` / `@form(ring_buffer)`: parsed but
       reject with "not yet implemented" diag (FORM-4 work).
     - Unknown form names: clear "unknown form `X`; v1
       recognizes: vec, hashmap, ring_buffer" diag.
     - 8 new tests: vec-shape-OK, vec-with-pool (reject),
       vec-no-capacity (reject), vec-multiple-slots
       (reject), vec-with-args (reject), hashmap-not-yet,
       unknown-form, non-form-locus regression guard.
     - 535 workspace tests passing (up from 527).

  3b. **PR3b — @form(vec) method synthesis.** Inject
     `push`/`get`/`pop`/`len`/`is_empty` signatures into the
     locus's method scope so call sites resolve. Also synthesize
     the `IndexError` payload type.

     **DONE 2026-05-12 (commit pending).**
     - `MethodInfo` gained `fallible: Option<Ty>` to mirror
       `FnSig.fallible`. All existing method-construction sites
       (mode methods, locus fn members, perspective fn members,
       perspective `is_stable`) updated.
     - In `resolve::register_locus`, after collecting user
       methods, `@form(vec)` loci get `push`/`get`/`pop`/`len`/
       `is_empty` synthesized via `synthesize_form_vec_methods`
       over the heap slot's cell type T. Shape verification
       (PR3a) catches malformed shapes; method synthesis is
       best-effort and falls back to `Ty::Unknown` for T when
       the shape is invalid.
     - `inject_form_stdlib_types` synthesizes `IndexError` as
       a struct `{ kind: String, index: Int, len: Int }` into
       the top scope when any locus in the bundle uses
       `@form(...)`. Idempotent — user-declared `IndexError`
       wins.
     - `Checker::callee_fallible_payload` extended to handle
       method calls (`Expr::Field` callee): looks up the
       receiver's type, finds the method by name in the locus
       (or perspective) `methods` list, returns its
       `fallible` field. So `l.get(0)` produces
       `Ty::Fallible { success: T, payload: IndexError }`
       and the caller is forced to address it.
     - 7 new tests: synthesized push resolves, get + or raise
       OK, get without `or` rejected with error-not-addressed,
       pop + or handler(err) with typed payload, len/is_empty
       synthesized, struct cell type T threads through,
       IndexError reachable as type in user code.
     - 542 workspace tests passing (up from 535).
  4. **PR4 — C runtime lotus_vec_*.** Standalone C with unit
     tests; no codegen integration.

     **DONE 2026-05-12 (commit pending).**
     - Added 7 functions to `lotus_arena.c` (~120 LoC):
       `lotus_vec_init`, `lotus_vec_push`,
       `lotus_vec_get` (int return: 1=OK, 0=out-of-bounds),
       `lotus_vec_pop` (same fallible-int convention),
       `lotus_vec_len`, `lotus_vec_is_empty`,
       `lotus_vec_destroy`.
     - Generic over T via `elem_size` parameter — all
       `lotus_vec_<T>_t` typedefs share the same
       `{ size_t cap, size_t len, char *buf }` prefix layout
       and the C helpers cast through that.
     - Doubling realloc: cap 0 → 4 → 8 → 16 → ...; initial
       4-element burst avoids per-element malloc for tiny
       vecs. Cap is monotonic in v1; shrink deferred.
     - OOM is currently a best-effort no-op on `push`; PR5/6
       will wire substrate trap → closure violation routing
       per `aperio-design-philosophy.md` § 2.
     - Compiles cleanly under `gcc -Wall -Wextra -Werror`.
     - 542 workspace tests still passing (no regressions —
       no codegen integration with the new fns yet).
  5. **PR5 — codegen for @form(vec).** Lower the heap slot to
     the vec struct; lower the synthesized methods to lotus_vec_*
     calls. End-to-end Aperio test that uses ItemListL.
  6. **PR6 — codegen for fallible / fail / or.** Lower the
     error-addressing surface; pick the fallible ABI shape
     (LLVM-level pair vs. tagged sum vs. side-channel).
  7. **PR7 — interpreter parity for vec + fallible.**

     **DONE 2026-05-12 (commit pending).**
     - `Value::FallibleErr(Box<Value>)` represents "an error
       occurred" at the value level. Produced by `Stmt::Fail`
       (Signal::Return wrapping the FallibleErr), consumed by
       `Expr::Or`.
     - `Stmt::Fail`: evaluates payload, returns
       `Signal::Return(Value::FallibleErr(payload))` which
       unwinds the fn body via the error path.
     - `Expr::Or`: evals inner. If `FallibleErr`, applies
       disposition — `Raise` → `Signal::Bubble(payload)` (the
       runtime's closure-violation routing); `Substitute(rhs)`
       binds `err` in env scope to the payload and evaluates
       rhs. If inner produced a regular value, passes through.
     - `SlotState::Vec { items: Rc<RefCell<Vec<Value>>> }` new
       variant. `instantiate_locus` detects `@form(vec)` and
       initializes the heap slot as a Vec instead of the
       default Heap state. Falls back to the existing Pool/Heap
       paths for non-form loci.
     - `try_eval_form_vec_call` intercepts method calls before
       normal dispatch — checks `receiver` is a `@form(vec)`
       locus AND method is one of push/get/pop/len/is_empty.
       Implements:
       - `push(x)` — append; returns `Unit`
       - `get(i)` — returns the element OR
         `Value::FallibleErr(IndexError)` on out-of-bounds
       - `pop()` — returns the last element OR
         `Value::FallibleErr(IndexError)` on empty
       - `len()` — Int
       - `is_empty()` — Bool
     - `index_error_value` helper synthesizes the
       `IndexError { kind, index, len }` struct payload to
       match the type the resolver injects.
     - 9 new end-to-end tests in
       `crates/aperio-runtime/tests/form_vec_interpreter.rs`:
       push→get round-trip, len + is_empty tracking, LIFO pop,
       get OOB with substitute, pop empty with substitute,
       get OOB with raise (bubbles to error), `err` binding
       payload usage, `fail` exits via error path with caller
       OR-handling, vec of struct cells.
     - 551 workspace tests passing (up from 542). The HTTP
       integration test is flaky (pre-existing) — passes on
       retry; unrelated to FORM work.
  8. **PR8 — microbench harness for FORM-3.**

  **Open spec questions in `spec/forms.md` ("Open questions
  deferred to FORM-2 / FORM-3"):** iteration surface, bulk ops,
  `set(i, x)` for in-place mutation. Decide during FORM-2 as
  the surrounding code shapes up.

- v1.x-FORM-3: benchmark `@form(vec)` against hand-written C —
  perf hypothesis (within 10%) is the gate before more forms.
- v1.x-FORM-4: ship `@form(hashmap)` + `@form(ring_buffer)`.

Friction-log impact:
- `reader-list_item-quadratic-concat` → STRING-BUILDER LANDED.
  Ferryman scale to multi-binary-grease unblocked at the
  primitive layer; remaining work is rewriting Reader to use
  builder_append instead of `buf = buf + chunk`.
- `type-records-cannot-hold-fn-pointer-fields` → LANDED.
- `dense-locus-storage-bloat` → SUBSTRATE-LANDED (v1.x-1/2).
- `http-request-headers-absent` case-insensitive follow-up →
  LANDED via std::str::lower.

## Concurrency note (2026-05-11 session)

Another process modified the repo during this session:
- moa/README.md appeared after v1.x-1 finished — got bundled
  into v1.x-2 commit via `git add -A`
- moa/MOA.md, moa/subjects.md, moa/types.ap, lib/README.md,
  crates/aperio-codegen/tests/moa_path_resolution.rs, and
  ~12 references to MOA_PATH_RENAMES / MOA_AP_SOURCE in
  codegen.rs appeared after v1.x-2 — got bundled into
  v1.x-4 commit

The moa-related work appears to be a parallel substrate
introduction. The moa_path_resolution.rs tests pass on the
v1.x-4 working tree, so the code is consistent — but the
v1.x-N commits accidentally contain unrelated moa changes.

For future sessions: BEFORE git add -A, verify no untracked
moa/* or lib/* files are unrelated to current task.
Alternatively use `git add <specific files>` for v1.x-N
commits going forward.
