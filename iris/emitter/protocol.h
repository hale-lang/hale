/* iris observation protocol v0 — the executable form now lives in the
 * hale tree, next to the emitter that implements it (GH #527 B1):
 *
 *   crates/hale-codegen/runtime/obs_protocol.h
 *
 * This file forwards so every existing include path in iris keeps
 * working. Edit the hale copy; there is no other. */
#include "../../crates/hale-codegen/runtime/obs_protocol.h"
