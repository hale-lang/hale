# Corpus effect baseline — the behavioural fingerprint of every
# in-tree example, one section per program.
#
# Regenerate with: scripts/effects-baseline.sh
#
# A diff here means some function's INFERRED effects changed.
# That is either intended (and the diff is the review artifact)
# or a regression annotations could never catch — a handler that
# quietly starts doing filesystem I/O changes no source you would
# think to look at, but it changes this file.

### 01-locus-with-run
TickerL::run  does={syscall,block,time}
main  does={alloc}
### 02-parent-child
CoordinatorL::accept  does={syscall}
CoordinatorL::run  does={alloc}
main  does={alloc}
### 03-closure-test
main  does={syscall,alloc}
### 03b-closure-absorbed
AuditL::run  does={syscall,alloc}
main  does={alloc}
### 03c-closure-bubbled
AuditL::run  does={alloc}
main  does={syscall,alloc}
### 04-modes
AggregatorL::run  does={syscall,alloc}
main  does={alloc}
### 05-bus
AckLogL::on_ack  does={syscall}
EchoL::on_greeting  does={syscall,publish,alloc}
SenderL::birth  does={publish,alloc}
main  does={alloc}
### 06-mutable-counter
main  does={syscall}
### 07-control-flow
main  does={syscall}
### 08-monotonic-sleep
main  does={syscall,block,time}
### 09-functions
announce  does={syscall}
main  does={syscall}
### 10-stateful-locus
Accumulator::run  does={syscall}
main  does={alloc}
### 11-drain-dissolve
ChildL::birth  does={syscall}
ChildL::dissolve  does={syscall}
ChildL::drain  does={syscall}
ParentL::birth  does={syscall}
ParentL::dissolve  does={syscall}
ParentL::drain  does={syscall}
ParentL::run  does={alloc}
main  does={alloc}
### 12-user-types
main  does={syscall,alloc}
### 13-decimal-and-exit
main  does={syscall}
### 14-projection-classes
ChunkedCoord::run  does={syscall,alloc}
RecognitionCoord::run  does={syscall,alloc}
RichCoord::run  does={syscall,alloc}
main  does={alloc}
### 15-match
classify  does={syscall}
echo  does={syscall}
main  does={syscall}
sign_of  does={syscall}
status_label  does={syscall}
### 16-schedule-classes
CooperativeWorker::run  does={syscall}
PinnedWorker::run  does={syscall,block,time}
ScheduleClassesApp::run  does={syscall}
main  does={alloc}
### 17-yield
TickLogger::on_tick  does={syscall}
TickProducer::run  does={syscall,publish,alloc}
main  does={alloc}
### 18-pinned-lifecycle
PinnedJob::birth  does={syscall}
PinnedJob::dissolve  does={syscall}
PinnedJob::drain  does={syscall}
PinnedJob::run  does={syscall,block,time}
PinnedJobApp::run  does={syscall}
main  does={alloc}
### 19-pinned-bus
PinnedBusApp::run  does={syscall,block,time}
PinnedSubscriber::birth  does={syscall}
PinnedSubscriber::dissolve  does={syscall}
PinnedSubscriber::on_tick  does={syscall}
Publisher::birth  does={publish,alloc}
main  does={alloc}
### 20-pinned-core
CoreOneWorker::run  does={syscall,block,time}
CoreWorkersApp::run  does={syscall}
CoreZeroWorker::run  does={syscall,block,time}
main  does={alloc}
### 20b-pinned-cores
CpusetApp::run  does={syscall}
RangeWorker::run  does={syscall,block,time}
SetWorker::run  does={syscall,block,time}
main  does={alloc}
### 20c-topology-numa
L3Worker::run  does={syscall,block,time}
NodeWorker::run  does={syscall,block,time}
TopologyApp::run  does={syscall}
main  does={alloc}
### 20d-replicas
ReplicaApp::run  does={syscall}
Worker::run  does={syscall,block,time}
main  does={alloc}
### 21-arrays
main  does={syscall,alloc}
### 22-moving-average
Sink::on_smoothed  does={syscall}
Source::birth  does={publish,alloc}
Window::on_sample  does={publish,alloc}
main  does={alloc}
### 23-ranges
main  does={syscall}
### 24-default-params
Counter::bump  does={syscall}
Counter::run  does={syscall}
greet  does={syscall}
main  does={syscall,alloc}
### 25-imports
main  does={syscall,alloc}
### 26-tuples
main  does={syscall}
### 27-strings
main  does={syscall,alloc}
shout  does={alloc}
### 28-to-string
label  does={alloc}
main  does={syscall,alloc}
summary  does={alloc}
### 29-helpers
main  does={syscall,alloc}
### 30-stats
Aggregator::on_sample  does={syscall,alloc}
Producer::birth  does={publish,alloc}
main  does={alloc}
### 31-birth-closures
AuditL::run  does={syscall,alloc}
Configured::birth  does={syscall}
main  does={alloc}
### 32-restart
Coordinator::run  does={syscall,alloc}
Worker::birth  does={syscall}
main  does={alloc}
### 33-quarantine
Coordinator::run  does={syscall,alloc}
Worker::run  does={syscall}
main  does={alloc}
### 34-quarantine-bus
Coordinator::run  does={syscall,publish,alloc}
FailingWatcher::on_sample  does={syscall}
HealthyWatcher::on_sample  does={syscall}
main  does={alloc}
### 35-tick-closures
Auditor::run  does={syscall,publish,alloc}
Counter::on_inc  does={syscall}
main  does={alloc}
### 36-duration-closures
Pulser::run  does={syscall,block,publish,time,alloc}
Watcher::on_tick  does={syscall}
main  does={alloc}
### 37-explicit-closures
Auditor::run  does={syscall,publish,alloc}
Ledger::on_post  does={syscall}
main  does={alloc}
### 38-restart-in-place
Coordinator::run  does={syscall,alloc}
Worker::birth  does={syscall}
main  does={alloc}
### 39-multi-instance-bus
Producer::run  does={syscall,publish,alloc}
Watcher::on_sample  does={syscall}
main  does={alloc}
### 40-pinned-duration
Heartbeat::run  does={syscall,block,time}
main  does={syscall,alloc}
### 41-closure-accumulator
Coordinator::run  does={syscall,publish,alloc}
Tracker::on_data  does={syscall}
main  does={alloc}
### 42-accumulator-vocab
Coordinator::run  does={syscall,publish,alloc}
Tracker::on_data  does={syscall}
main  does={alloc}
### 43-enums
main  does={syscall}
report  does={syscall}
### 44-decimal-exact
main  does={syscall}
### 45-enum-payloads
handle  does={syscall}
main  does={syscall}
report  does={syscall}
### 46-fn-arenas
decorate  does={alloc}
main  does={syscall,alloc}
pair  does={alloc}
### 47-fn-arenas-extras
main  does={syscall,alloc}
make_arr  does={alloc}
make_point  does={alloc}
### 48-publish-during-dissolve
Receiver::on_farewell  does={syscall}
Sender::dissolve  does={syscall,publish,alloc}
main  does={syscall,alloc}
### 49-fn-handle-rooting
Pulse::birth  does={publish,alloc}
Watcher::on_evt  does={syscall}
main  does={syscall,alloc}
make_pair  does={alloc}
### 50-mode-defaults
AggregatorL::run  does={syscall}
main  does={alloc}
### 51-geom-segment
main  does={syscall,alloc}
near_eq  does={syscall}
### 52-window-decay
main  does={syscall,alloc}
near_eq  does={syscall}
### 53-window-ring
Ring::clear  does={alloc}
Ring::push  does={alloc}
main  does={syscall,alloc}
near_eq  does={syscall}
### 54-geom-leading-edge
LeadingEdge::clear  does={alloc}
LeadingEdge::push  does={alloc}
main  does={syscall,alloc}
near_eq  does={syscall}
### 55-geom-triangulate
LeadingEdge::push  does={alloc}
Triangulator::run  does={syscall,alloc}
main  does={alloc}
near_eq  does={syscall}
### 56-io-frame-line
main  does={syscall,alloc}
### 57-geom-velocity
main  does={syscall,alloc}
near_eq  does={syscall}
### 58-stat-correlate
main  does={syscall,alloc}
near_eq  does={syscall}
### 59-locality-budget
App::run  does={syscall}
main  does={alloc}
### 60-lru-cache
expect  does={syscall}
expect_false  does={syscall}
main  does={syscall,alloc}
### 60-perspective-slot
App::run  does={syscall}
Gateway::run  does={syscall}
main  does={alloc}
### 61-reperspective-swap
App::run  does={syscall}
Gateway::run  does={syscall}
main  does={alloc}
### 62-perspective-bus-contract
App::run  does={syscall}
RouterV1::on_order  does={syscall}
main  does={alloc}
### 63-reperspective-migrate
App::run  does={syscall}
Service::run  does={syscall}
main  does={alloc}
### 64-perspective-bus-swap
OrderV1::on_order  does={syscall}
OrderV2::on_order  does={syscall}
Service::run  does={syscall,publish,alloc}
main  does={alloc}
### 65-hashmap-anchor-churn
HashmapAnchorChurn::run  does={syscall,alloc}
HashmapAnchorChurn::step  does={alloc}
main  does={alloc}
### 66-self-field-retire
SelfFieldRetire::alias_literal  does={alloc}
SelfFieldRetire::alias_struct  does={alloc}
SelfFieldRetire::churn  does={alloc}
SelfFieldRetire::direct  does={alloc}
SelfFieldRetire::run  does={syscall,alloc}
main  does={alloc}
### 67-match-expression
main  does={syscall,alloc}
### 68-hot-certified
App::run  does={syscall}
Summer::add_range  alloc_per_call=0
main  does={alloc}
### 69-http-router
Count::handle  does={alloc}
Hello::handle  does={alloc}
Stamp::after  does={alloc}
main  does={syscall,alloc}
### 70-cell-single-owner
CellSingleOwner::check  does={alloc}
CellSingleOwner::churn_bytes  does={alloc}
CellSingleOwner::churn_cells  does={alloc}
CellSingleOwner::churn_name  does={alloc}
CellSingleOwner::mutate_source  does={alloc}
CellSingleOwner::run  does={syscall,alloc}
CellSingleOwner::seed  does={alloc}
main  does={alloc}
### 71-conditional-instantiation
App::run  does={syscall}
Ghost::run  does={syscall}
Worker::run  does={syscall}
main  does={syscall,alloc}
### 72-teardown-publish-delivery
App::run  does={syscall}
Fetcher::run  does={syscall,block,publish,time,alloc}
Tally::dissolve  does={syscall}
main  does={alloc}
### 73-bounded-bus
App::run  does={syscall,block,time}
Full::dissolve  does={syscall}
Pusher::run  does={syscall,block,publish,time,alloc}
Sampler::dissolve  does={syscall}
main  does={alloc}
### 74-effect-contracts
App::birth  does={syscall,publish,alloc,money}
charge  does={money}
double  none={block,syscall}
main  does={alloc}
quote  none={money,syscall}
safe  none={panic}
scale  none={entropy,env,time}  alloc_per_call=0
### 85-bindings-unix
App::run  does={syscall,block,time}
Counter::on_beat  does={syscall}
main  does={alloc}
### docs-server
__docs_dir  does={env}
__render_doc  does={syscall,env,alloc}
__render_index  does={syscall,env,alloc}
__wrap_html_page  does={alloc}
handle_request  does={syscall,block,env,alloc}
main  does={syscall,env,alloc}
### fitter-applier-demo
ActionLogL::on_action  does={syscall}
ApplierL::on_kernel  does={syscall}
ApplierL::on_observation  does={publish,alloc}
FitterL::on_observation  does={publish}
ObservationSourceL::birth  does={publish,alloc}
main  does={alloc}
### hello-world
HelloL::birth  does={syscall}
main  does={alloc}
### http-hello
handle_request  does={syscall,block,alloc}
main  does={syscall,env,alloc}
### io-demo
main  does={syscall,env,alloc}
### multi-file-seed
main  does={alloc}
