### Every performer declares an effect class (GH #946)

- **DNA legs: every performer declares an effect class** (GH #946, from the outside review of the voice split). `effect_free`, `idempotent` or `uncertain`, with no default: a catalog with a performer declaring none is refused when the leg starts, and a project's own `dna/org/work.hl` must declare (`hale dna new` seeds `Person { effect: "idempotent" }`). The class is the claim's filter — a judgment or an analysis admits `effect_free` and `idempotent`, an edit `idempotent` and `uncertain`, anything else all three; the hat says so in `effects` — and evidence on the outcome (`effect_class`). A failed settle or a lost model reply behind an `uncertain` performer is `unresolved`: friction filed naming the attempt, the request id and the lease, the lease kept, the loop's slot stopped, nothing retried by a program; behind the other two it is `unsettled` (the loop runs again) or a failed outcome. `hale dna work next|submit --effect <class>`; `effect_class` on the claim and outcome wire and in contract v1.
