// types.ap — typed records shared across files in this
// directory. F.19 per-directory seed model: every `.ap` file
// in this dir is in one shared scope, so `Trade` is visible
// to main.ap and notional.ap without any `import` statement.

type Trade {
    symbol: String;
    price:  Decimal;
    qty:    Decimal;
}
