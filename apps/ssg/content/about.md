# About

The SSG reads `.md` files from an input directory and writes
matching `.html` files to an output directory.

## Notes

Inline markdown (bold, italics, links) is not yet supported by
the stdlib renderer — it is a v0 block-level pass only.
