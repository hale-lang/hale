# `std::name`

> std::name::Convention — orthography helpers (snake_case, CamelCase, file-stem ↔ locus-name).

The source for this module is the documentation; the page is regenerated by
`docs/scripts/regen-std-source.py` from the live stdlib source.

```aperio
{{#include ../../../crates/aperio-codegen/runtime/stdlib/name.ap}}
```
