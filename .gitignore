# Generated parsers / compiler output
build/
target/
*.o
*.so
*.dylib

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Working notes (untracked by default; promote to notes/ when stable)
.scratch/

# Rust build artifacts
/target/
# Cargo.lock IS committed (binary workspace, reproducible builds).

# mdbook build output (per-subtree). Source lives in docs/<tree>/src/.
docs/quickstart/book/
docs/grimoire/book/
docs/book/book/
docs/reference/book/
docs/std/book/
